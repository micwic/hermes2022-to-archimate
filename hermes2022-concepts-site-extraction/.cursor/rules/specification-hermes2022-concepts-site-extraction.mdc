---
description: Spécification générale du module hermes2022-concepts-site-extraction
alwaysApply: true
---

# specification-hermes2022-concepts-site-extraction.mdc: Spécification générale du module hermes2022-concepts-site-extraction

Chemin : hermes2022-to-archimate/hermes2022-concepts-site-extraction/.cursor/rules/

> Créé le : 2025-08-16
> Dernière mise à jour : 2025-10-24

## Description générale

- La méthode HERMES des gestion de projet et de programme de la Confédération Helvétique est décrite en anglais sur le site <https://www.hermes.admin.ch/en/>
- **Finalité du module** : Extraire automatiquement les concepts HERMES2022 depuis les pages officielles spécifiques et les transformer en fichiers intermédiaires structurés JSON validés
- **Portée du module** : Extraction ciblée des concepts de la méthode HERMES2022 avec leur contexte et articulation générale, selon la hiérarchie officielle (1=Initiation, 2=Execution avec 2.1=Concept, 2.2=Implementation, 2.3=Deployment, 3=Closure)
- **Interface de sortie** : Fichiers JSON conformes aux schémas définis dans le module hermes2022-extraction-files

## Contexte

- Spécification amont @.cursor/rules/specification-hermes2022-to-archimate.mdc
- Les méthodes HERMES de gestion de portefeuille et de gestion des applications sont hors contexte, elles sont ignorées
- La méthode HERMES2022 de gestion de projet et de programme de la Confédération Helvétique est décrite en plusieurs langues : allemand, français, italien et anglais
- La racine du site <https://www.hermes.admin.ch> est modifiée automatiquement à la langue par défaut ou est celle définie dans l'url lors de la requête
- L'url du site en allemand est <https://www.hermes.admin.ch/de/>
- L'url du site en français est <https://www.hermes.admin.ch/fr/>
- L'url du site en italien est <https://www.hermes.admin.ch/it/>
- L'url du site en anglais est <https://www.hermes.admin.ch/en/>

## Objectifs

- Faire extraire par l'IA les concepts HERMES2022 jugés pertinents pour le projet des pages identifiées comme pertinentes du site internet de référence dans la langue souhaitée.
- Faire transformer par l'IA les données jugées pertinentes pour les enregistrer dans des fichiers intermédiaires structurés, lisibles et vérifiables par un humain avant chargement ultérieur

## Conception générale

- Un fichier de configuration JSON de l'extraction contient les propriétés utiles à celle-ci
- Le processus d'extraction est un agent spécialisé :<br>- Javascript compatible comme plugin JArchi de Archi<br>- générique et configurable, son exécution conforme est basée sur le contenu du fichier de configuration et sur les définitions de structure du fichier intermédiaire cible
- L'extraction des contenus du site internet est assurée par l'agent spécialisé à partir des urls définies pour chaque concept dans le fichier de configuration
- Les transformations sont effectuées par l'IA spécialisée NuExtract en faisant appel aux APIs disponibles en mode SaaS dans un projet nommé HERMES2022 qui peut comprendre des exemples ajustables propres à chaque contexte directement dans la plateforme SaaS en dehors du code de l'agent.
- Les concept HERMES2022 jugés pertinents sont, en plus de la vue générale de la méthode dans son ensemble : 1) Les phases, 2) Les scénarios, 3) Les modules, 4) Les tâches, 5) Les résultats, 6) Les rôles
- les structures cibles de la transformation sont les structures et types définis pour les fichiers intermédiaires JSON

## Structure de répertoire

| Élément | Type | Description |
|:--------|:-----|:------------|
| `/src/` | Répertoire | Code source de l'agent JavaScript pour l'extraction via APIs NuExtract |
| `/src/nuextract-client.js` | Code | Client pour les requêtes HTTP vers APIs NuExtract et gestion des réponses JSON |
| `/config/` | Répertoire | Configuration de l'extraction et paramètres pour l'agent JS |
| `/config/extraction-config.json` | Configuration | Paramètres de configuration pour l'extraction automatisée (clés API, URLs, etc.) |

## Décisions architecturales et techniques

### Site internet de référence HERMES2022

#### Justification pour le site internet de référence HERMES2022 en anglais

- La documentation de référence du projet est en anglais, les modèles Archimate sont donc en anglais, le site de référence à prendre en considération pour HERMES2022 celui en anglais (<https://www.hermes.admin.ch/en/>).

#### Description détaillée pour le site internet de référence HERMES2022

- [2025-08-16] La racine du site internet de référence pour l'extraction des concepts HERMES2022 est <https://www.hermes.admin.ch/en/>. Elle permet de définir la version de la méthodologie.
- [2025-10-16] La vue générale de la méthodologie est décrite dans les pages : <br>- <https://www.hermes.admin.ch/en/project-management/method-overview.html><br>- <https://www.hermes.admin.ch/en/project-management/method-overview/hermes-project-management-method-components.html><br>- <https://www.hermes.admin.ch/en/project-management/method-overview/preface.html>
- [2025-08-16] Les phases sont décrites dans les pages : <br>- <https://www.hermes.admin.ch/en/project-management/phases.html><br>- <https://www.hermes.admin.ch/en/project-management/phases/initiation.html><br>- <https://www.hermes.admin.ch/en/project-management/phases/concept.html><br>- <https://www.hermes.admin.ch/en/project-management/phases/implementation.html><br>- <https://www.hermes.admin.ch/en/project-management/phases/deployment.html><br>- <https://www.hermes.admin.ch/en/project-management/phases/execution.html><br>- <https://www.hermes.admin.ch/en/project-management/phases/closure.html>

#### Patterns validés à suivre suite à la décision

- **URL RACINE** à utiliser pour les requêtes internet est <https://www.hermes.admin.ch/en/>

#### État d'implémentation de la décision

🚧 [En cours de spécification et réalisation]

### Approche hybride IA : Javascript et modèle LLM spécialisé NuExtract dans le cloud par API

#### Description détaillée pour l'approche hybride IA

- [2025-10-14] L'extraction des concepts HERMES2022 utilise une approche hybride en deux phases : Phase 1 avec l'extraction de pages internet de références par Javascript qui aliment des appels à l'IA spécialisée NuExtract par les APIs disponibles dans le cloud pour synthétiser les contenus et assurer la transformation dans le format JSON.
- [2025-08-16] L'approche doit permettre la réutilisation des comportements dans d'autres contextes que le projet actuel, notamment avec du paramétrage (autres contenants et contenus, autres instructions pour l'IA).
- [2025-10-14] Le composant doit pouvoir s'exécuter dans Archi avec le plugin JArchi

#### Justification pour l'approche hybride IA avec NuExtract

- NuExtract est un LLM spécialisé pour l'extraction de données à partir de différentes sources pour transformation dans un format structurés tel que JSON
- Une plateforme SaaS est disponible et gratuite avec de faible volume de données

#### Patterns validés pour l'approche hybride IA

Exemples de la documentation officielle :

```bash
 curl "https://nuextract.ai/api/projects" \
  -H "Authorization: Bearer $NUEXTRACT_API_KEY"
```

```bash
PROJECT_ID="4c64870d-da8c-4718-aea9-fb2f86f07b42"
curl "https://nuextract.ai/api/projects/$PROJECT_ID/extract" \
  -X "POST" \
  -H "Authorization: Bearer $NUEXTRACT_API_KEY" \
  -H "content-type: text/plain" \
  -d "This is a text"
```

```bash
  PROJECT_ID="4c64870d-da8c-4718-aea9-fb2f86f07b42"
curl "https://nuextract.ai/api/projects/$PROJECT_ID/extract" \
  -X "POST" \
  -H "Authorization: Bearer $NUEXTRACT_API_KEY" \
  -H "content-type: application/octet-stream" \
  --data-binary @file_name.ext
```

L'ensemble des APIs NuExtract sont décrites dans @hermes2022-concepts-site-extraction/.cursor/rules/nuextract-platform.yaml qui est la référence officielle à utiliser

#### Anti-Patterns pour l'approche hybride IA à éviter

- Néant

#### État d'implémentation pour l'approche hybride IA

🚧 [En cours de conception et réalisation]

### Utilisation d'un agent JavaScript pour l'extraction via APIs NuExtract

#### Description détaillée pour l'utilisation d'un agent JavaScript

- [2025-10-14] L'extraction des concepts HERMES2022 est réalisée par un agent JavaScript exécuté dans JArchi (plugin d'Archi), qui articule les requêtes HTTP vers les APIs de NuExtract dans le cloud pour analyser et résumer le contenu des pages HTML, puis générer des fichiers JSON structurés avec les synthèses opérées.
- [2025-10-14] L'agent gère le cycle complet : récupération des pages via HTTP, appel à NuExtract pour extraction/synthèse, validation et écriture des templates générés.
- [2025-10-14] L'agent est conçu pour être modulaire, réutilisable et intégré à l'écosystème JArchi de Archi sans dépendances externes inutiles.

#### Justification pour l'utilisation d'un agent JavaScript

- **Simplicité et Intégration** : Langage de référence qui permet d'utiliser les APIs Java natives (java.net.http.HttpClient) pour les requêtes HTTP et les appels aux APIs de NuExtract, évitant les dépendances Node.js ou Axios, et permettant une exécution directe dans JArchi de Archi.
- **Limitation plateforme SaaS** : L'utilisation d'un agent permet d'assurer une meilleure maitrise des templates NuExtract en contournant certaines limitations de l'assistant de transformation JSON->Template de la plateforme qui ne traite pas complètement les énumérations décrites dans les formats JSON par exemple

#### Patterns validés pour l'utilisation d'un agent JavaScript

- **Requêtes HTTP natives** : Utilisation de java.net.http.HttpClient pour les extractions de pages internet et les appels vers NuExtract, avec gestion des timeouts et redirections.
- **Gestion asynchrone** : Promesses JavaScript pour les requêtes non-bloquantes.
- **Validation JSON** : Utilisation de Zod ou Ajv pour valider les réponses NuExtract contre les schémas JSON.

#### Anti-Patterns pour l'utilisation d'un agent JavaScript à éviter

- **Dépendances externes lourdes** : Éviter Axios ou libs Node.js → **Solution** : APIs Java natives → **Règle à adopter** : Minimalisme pour intégration JArchi.
- **Gestion d'erreurs insuffisante** : Éviter les timeouts infinis → **Solution** : Timeouts explicites → **Règle à adopter** : Robustesse et non-blocage de l'UI.

#### État d'implémentation pour l'utilisation d'un agent JavaScript

🚧 [En cours de conception et réalisation]

### Intégration des APIs NuExtract pour l'extraction structurée

#### Description détaillée pour l'intégration des APIs NuExtract

- [2025-10-14] L'agent JavaScript utilise les APIs de NuExtract (modèle open source 3.8B basé sur Phi-3-mini) pour extraire des données structurées depuis du texte ou HTML vers JSON, garantissant un JSON valide et respectueux des schémas.
- [2025-10-16] Les APIs NuExtact sont décrites dans @/home/micwic/gitRepositories/hermes2022-to-archimate/hermes2022-concepts-site-extraction/.cursor/rules/nuextract-platform.yaml
- [2025-10-16] GET /api/projects permet de lister les projets disponibles pour le compte dans dans la plateforme SaaS nuextact.ai, cela permet de déterminer {PROJECT_ID} pour HERMES2022 si il existe déjà dans la plateforme SaaS nuextact.ai pour le compte.
- [2025-10-16] La clé API valable est stockée dans @hermes2022-concepts-site-extraction/config/nuextract-api-key.key
- [2025-10-16] POST /api/projects permet de créer un projet et de déterminer {PROJECT_ID} pour HERMES2022 si un tel projet n'existe pas encore dans la plateforme SaaS nuextact.ai. Le projet est créé avec son template associé qui doit exister au préalable dans le format requis par NuExtract
- [2025-10-16] POST /api/infer-template permet de créer un Template NuExtract à partir d'une description textuelle. Dans le contexte du projet, il faut fusionner les instructions textuelles de @/home/micwic/gitRepositories/hermes2022-to-archimate/hermes2022-concepts-site-extraction/config/instructions-template-nuextract.md avec les formats JSON pertinents, avec y compris les éventuelles références (Exemple : "phases": {"$ref": "./hermes2022-phases.json"}) dans un seul format JSON homogène.
- [2025-10-16] PUT /api/projects/{projectId}/template permet de mettre à jour le template pour un projet déjà existant
- [2025-10-17] POST /api/projects/{projectId}/infer-text permet d'exécuter une extraction sur la base d'un texte dans le cadre fixé pour le projet à savoir sur la base d'un template NuExtract et des exemples de transformation fournis pour les paramètres définis. Dans le contexte du projet, il faut fusionner les instructions textuelles conformément à la séquence définie dans @hermes2022-concepts-site-extraction/config/extraction-hermes2022-main.md avec avec les contenus HTML extrait des pages de références correspondantes
- [2025-10-14] Gestion des quotas : L'API supporte des requêtes gratuites avec limites (ex. : 1000 requêtes/mois), et payantes pour plus de volume ; l'agent inclut un suivi des usages.

#### Justification pour l'intégration des APIs NuExtract

- **Précision et Fiabilité** : Modèle spécialisé pour extraction HTML→JSON, surpassant les modèles généralistes pour cette tâche, avec validation automatique du JSON.
- **Sécurité et Conformité** : Utilisation de la clé API personnelle (pas de stockage sensible externe), respect des licences MIT et lois suisses.
- **Intégration avec JArchi** : APIs REST simples, compatibles avec java.net.http.HttpClient, sans dépendances supplémentaires.
- **Évolutivité** : Support de schémas personnalisés pour différents concepts HERMES2022, et possibilité d'extension à d'autres modèles IA.

#### Patterns validés pour l'intégration des APIs NuExtract

- **Authentification Bearer** : Utilisation de l'en-tête `Authorization: Bearer <clé JWT>` pour sécuriser les requêtes avec la clé définie dans @hermes2022-concepts-site-extraction/config/nuextract-api-key.key
- **Gestion des Quotas** : Implémentation d'un compteur d'usage dans l'agent, avec logging pour éviter les dépassements.
- **Validation de Réponse** : Parsing de la réponse JSON avec vérification des erreurs (ex. : code 429 pour quota dépassé).

#### Anti-Patterns pour l'intégration des APIs NuExtract à éviter

- **Utilisation sans authentification** : Éviter les requêtes non authentifiées → **Solution** : Toujours inclure la clé Bearer → **Règle à adopter** : Sécurité des accès API.
- **Dépassement de quotas** : Éviter les requêtes excessives → **Solution** : Suivi et limites → **Règle à adopter** : Gestion proactive des ressources.
- **Hardcodage de la clé** : Éviter de coder la clé en dur → **Solution** : Variable d'environnement ou config → **Règle à adopter** : Sécurité et maintenabilité.

#### État d'implémentation pour l'intégration des APIs NuExtract

🚧 Intégration dans l'agent JS en cours

### Gestion des modes de génération de template (sync/async)

#### Description détaillée pour la gestion des modes

- [2025-10-24] L'extraction supporte deux modes de génération de template via le paramètre `templateMode` dans `extraction-config.json`
- [2025-10-24] **Mode sync** : Utilise `/api/infer-template` - traitement synchrone avec timeout HTTP global
- [2025-10-24] **Mode async** : Utilise `/api/infer-template-async` - traitement asynchrone avec polling de statut
- [2025-10-24] **Constat empirique** : L'API synchrone timeout à ~11 secondes avec le schéma `hermes2022-concepts.json` (4442 caractères) et retourne erreur 206 (Partial Content) avec body vide `{}`, tandis que l'API asynchrone permet de traiter le même schéma en 44 secondes avec succès
- [2025-10-24] **Paramètre unifié `templateGenerationDuration`** : Représente le temps estimé de génération (défaut 30000ms)
  - **Mode sync** : Utilisé comme base pour le timeout HTTP avec marge de sécurité = `templateGenerationDuration + 5000`
  - **Mode async** : Utilisé comme sleep initial avant le polling = `templateGenerationDuration`
- [2025-10-24] Le template généré est sauvegardé dans `shared/hermes2022-extraction-files/config/nuextract-template-generated/nuextract-template.json`

#### Justification pour la gestion des modes

- **Flexibilité** : Permet de choisir entre rapidité (sync pour petits schémas) et fiabilité (async pour schémas complexes >4000 caractères)
- **Résout les timeouts** : Le mode async résout les timeouts systématiques avec des schémas JSON complexes
- **Gestion d'erreur explicite** : Message d'erreur clair en cas de timeout sync proposant de passer en mode async

#### Patterns validés pour la gestion des modes

- **Paramètre `templateMode`** : Valeurs acceptées "sync" ou "async" (défaut "sync" si absent)
- **Paramètre `templateGenerationDuration`** : Temps estimé en ms (défaut 30000)
- **Paramètres d'endpoints** : `infer-templatePath` et `infer-templateAsyncPath` dans configuration
- **Mode sync** :
  - Timeout HTTP = `templateGenerationDuration + 5000` ms
  - Pas de sleep, pas de retry
  - Template retourné directement dans la réponse
  - Message d'erreur timeout explicite : "Timeout sync après Xms. Pour schémas >4000 caractères, utilisez templateMode: 'async'"
- **Mode async** :
  - Timeout paramètre API = 60 secondes (`?timeout=60s`)
  - Sleep initial = `templateGenerationDuration` ms
  - Polling interval = 3 secondes entre chaque vérification
  - Max attempts = 20 tentatives de polling
  - Workflow : POST `/api/infer-template-async?timeout=60s` → obtenir jobId → sleep → polling GET `/api/jobs/{jobId}` → récupérer outputData
  - Statuts acceptés : `"completed"` (succès normal) ET `"timeout"` (succès avec dépassement timeout mais outputData présent)
  - Statut échec : `"failed"` (échec du job)
- **Format description** : Schéma JSON en premier, puis instructions (sans headers/commentaires) — Les instructions placées avant le schéma perturbent la génération du template
- **Limite taille** : 32k caractères pour le champ `description` selon support NuMind

#### Anti-Patterns pour la gestion des modes à éviter

- **Mode invalide** : Éviter templateMode ni "sync" ni "async" → **Solution** : Validation stricte → **Règle à adopter** : Lever erreur explicite si valeur invalide
- **Utiliser mode sync pour schémas >4000 caractères** : Éviter `/api/infer-template` pour gros schémas → **Solution** : Utiliser mode async → **Règle à adopter** : Workflow async pour schémas complexes
- **Polling immédiat (async)** : Éviter de poller dès la soumission → **Solution** : Sleep `templateGenerationDuration` initial → **Règle à adopter** : Laisser le temps au job de s'initialiser
- **Polling trop rapide (async)** : Éviter interval <2s → **Solution** : Interval 3s → **Règle à adopter** : Limiter la charge sur l'API
- **Instructions avant schéma** : Éviter headers/instructions avant le schéma JSON → **Solution** : Schéma d'abord, instructions après → **Règle à adopter** : Format description = schéma + instructions (sans headers)

#### État d'implémentation pour la gestion des modes

✅ [Fonctionnalité réalisée]

### Parsing des instructions complémentaires depuis fichier Markdown

#### Description détaillée pour le parsing des instructions complémentaires

- [2025-10-21] Les instructions complémentaires pour l'API NuExtract sont stockées dans un fichier Markdown structuré avec documentation humaine et instructions opérationnelles séparées
- [2025-10-21] Le parsing extrait uniquement le contenu sous le heading `## Instructions complémentaires pour /api/infer-template /api/infer-template-async de NuExtract`
- [2025-10-21] La documentation (Objectif, Mode d'utilisation) est préservée pour la lisibilité humaine mais exclue de l'envoi à l'API
- [2025-10-21] Le parsing s'arrête au prochain heading de niveau 1 ou 2 (ou fin de fichier) pour isoler uniquement les instructions pertinentes

#### Justification pour le parsing des instructions complémentaires

- **Lisibilité préservée** : Le fichier reste documenté et compréhensible pour les humains
- **API optimisée** : Seules les instructions pertinentes sont envoyées, évitant la pollution du contexte
- **Maintenabilité** : Séparation claire entre documentation et instructions opérationnelles dans un fichier unique
- **Robustesse** : Fallback sur le fichier complet si le heading n'est pas trouvé

#### Patterns validés pour le parsing des instructions complémentaires

- **Heading standard** : `## Instructions complémentaires pour /api/infer-template /api/infer-template-async de NuExtract` comme marqueur de section
- **Parsing par lignes** : Extraction ligne par ligne depuis le heading jusqu'au prochain heading de niveau 1-2
- **Regex arrêt** : Pattern `/^#{1,2}\s+/` pour détecter les headings de niveau 1 ou 2
- **Fallback documenté** : Warning si heading non trouvé, utilisation fichier complet
- **Trim final** : Nettoyage des espaces/lignes vides avant/après

#### Anti-Patterns pour le parsing des instructions complémentaires à éviter

- **Fichiers séparés** : Éviter duplication doc/instructions → **Solution** : Fichier unique avec parsing → **Règle à adopter** : Documentation et instructions colocalisées
- **Envoi complet** : Éviter d'envoyer toute la documentation à l'API → **Solution** : Extraction ciblée → **Règle à adopter** : Parser le Markdown pour isoler les instructions
- **Heading variable** : Éviter des noms de heading changeants → **Solution** : Heading standard documenté → **Règle à adopter** : Convention de nommage stricte

#### État d'implémentation pour le parsing des instructions complémentaires

✅ [Fonctionnalité réalisée]

### Structure et format des instructions d'extraction

#### Description détaillée pour la structure et format des instructions d'extraction

- [2025-08-16] Les instructions d'extraction utilisent le format Markdown pour optimiser l'utilisation par l'IA
- [2025-08-16] Un fichier d'instructions séparé est créé pour chaque concept HERMES2022 à extraire (phases, scénarios, modules, tâches, résultats, rôles)
- [2025-08-16] Les instructions se concentrent uniquement sur la transformation, pas sur la définition de la structure cible
- [2025-08-16] La structure cible est définie par les schémas JSON des fichiers intermédiaires, créant un contrat d'interface entre extraction et consommation
- [2015-10-18] Les pages html de référence pour un concept sont déclarées dans une propriété sourceURL de type caractère associée à une énumération pour la liste qui est à concaténée pour la former la chaîne de caractère à l'exécution pour journaliser les pages effectivement utilisées lors de la création du fichier intermédiaire. Le schéma JSON sert à la fois de définition de la propriété de journalisation et de paramètre avec l'énumération.

#### Justification pour la structure et format des instructions d'extraction

- **Format Markdown** : Naturel pour l'IA, instructions conversationnelles claires
- **Fichiers séparés** : Maintenance facilitée, lisibilité optimale, évolution indépendante par concept
- **Séparation des responsabilités** : Instructions d'extraction ≠ définition structure cible
- **Contrat d'interface** : Synchronisation progressive entre instructions et schémas cibles

#### Patterns validés pour la structure et format des instructions d'extraction

- **Un fichier par concept** : `/config/extraction-{concept}.md` pour chaque concept HERMES2022
- **Instructions conversationnelles** : Prompts naturels adaptés à l'IA
- **Synchronisation progressive** : Évolution conjointe des instructions et des schémas cibles
- **Validation par schéma** : Contrôle de conformité via JSON Schema des fichiers intermédiaires

#### Anti-Patterns pour la structure et format des instructions d'extraction à éviter

- **Fichier monolithique** : Éviter un seul fichier pour tous les concepts → **Solution** : Fichiers séparés par concept → **Règle à adopter** : Modularité conceptuelle
- **Mélange des responsabilités** : Éviter de définir la structure cible dans les instructions → **Solution** : Séparation claire extraction/structure → **Règle à adopter** : Contrat d'interface via schémas
- **Format technique** : Éviter JSON/YAML pour les instructions → **Solution** : Markdown conversationnel → **Règle à adopter** : Instructions naturelles pour l'IA

#### État d'implémentation pour la structure et format des instructions d'extraction

🚧 [En cours de conception et réalisation]

### Contrat d'interface extraction-fichiers intermédiaires

#### Description détaillée pour le contrat d'interface

- [2025-08-16] Un contrat d'interface est établi entre les instructions d'extraction et les schémas des fichiers intermédiaires, il consiste surtout à vérifier et faire se synchroniser les définitions dans les spécifications respectives par l'IA
- [2025-08-16] La synchronisation se fait progressivement au fur et à mesure de l'évolution des besoins
- [2025-08-16] Les schémas JSON des fichiers intermédiaires définissent la structure cible de la transformation

#### Justification pour le contrat d'interface

- **Cohérence** : Garantit l'alignement entre extraction et consommation
- **Évolutivité** : Permet l'adaptation progressive des deux côtés
- **Validation** : Assure la conformité des données extraites
- **Maintenance** : Facilite la gestion des changements

#### Patterns validés pour le contrat d'interface

- **Synchronisation progressive** : Évolution conjointe des instructions et schémas
- **Validation automatique** : Contrôle de conformité via JSON Schema
- **Documentation partagée** : Référence commune pour l'équipe

#### État d'implémentation pour le contrat d'interface

🚧 [En cours de spécification et réalisation]

### Visa humain d'overview pour hermes2022-concepts

#### Description détaillée pour le visa humain d'overview

- [2025-08-19] La qualité du champ `overview` des concepts HERMES2022 est validée manuellement via un fichier d'approbation (sidecar) adjacent à l'artefact JSON produit.
- [2025-08-19] Le visa est consigné par éléments/règles à contrôler (statuts par item) avec des métadonnées globales uniques (reviewer, date, notes).

#### Justification pour le visa humain d'overview

- La conformité structurelle est assurée par JSON Schema; la qualité sémantique de `overview` requiert un jugement humain.
- La matérialisation explicite du visa améliore la traçabilité et l'auditabilité sans sur‑ingénierie.

#### Patterns validés pour le visa humain d'overview

- **Sidecar d'approbation**: Fichier sidecar adjacent nommé comme l'artefact avec suffixe `.approval.json` généré par l'IA en post extraction
- **Schéma d'approbation**: `shared/hermes2022-extraction-files/config/json-schemas/hermes2022-concepts-approval.json`.
- **Structure du visa**:
  - Métadonnées globales: `artifact`, `reviewer`, `date`, `notes?`.
  - Détails par item: `target`, `rule`, `status` (`approved|rejected|pending`), `lastChecked`, `comment?`, `evidence?`.
- **Initialisation status approbation** : Cible à valider générées selon liste ci-dessous avec status "pending" pour les cibles à valider en post extraction

**Cibles à valider par revue humaine (format JSON Pointer)** :

- `/concepts/overview`

#### Anti-Patterns pour le visa humain d'overview à éviter

- **Visa global unique**: éviter un statut global sans granularité par règle/cible.
- **Absence de schéma**: éviter des fichiers d'approbation non validés.

#### État d'implémentation pour le visa humain d'overview

✅ Schéma d'approbation créé: `shared/hermes2022-extraction-files/config/json-schemas/hermes2022-concepts-approval.json`
🚧 Intégration dans le flux d'extraction/validation BDD à compléter

### Paramétrage du répertoire des artefacts (artifactBaseDir)

#### Description détaillée

- [2025-08-20] Ajout d’une propriété `artifactBaseDir` dans `config/extraction-config.json` pour orienter l’outillage (tests BDD inclus) vers un répertoire unique d’artefacts sans recherche multiple.
- Valeur: chemin relatif au répertoire `hermes2022-concepts-site-extraction/` (chemin absolu accepté).
- Objectif: permette l'exécution avec des fixtures pour attester les fonctionnement des tests sans créer de sur consommation de ressources (alternative systématique avec fallback)

#### Justification

- Sûreté: les tests restent strictement conformes aux schémas; traçabilité inchangée.
- Durabilité: supprime les recherches/fallbacks redondants (économie CPU/IO).
- Gouvernance: distingue explicitement les jeux d’attestation (`__tests__/fixtures/...`) des données “réelles” (`shared/.../data/`) par configuration, pas par heuristique.

#### Patterns validés

- Répertoire unique piloté par `artifactBaseDir`.
- Sidecar d’approbation adjacent et résolu depuis le même répertoire.
- Override optionnel via variable d’environnement `HERMES_CONCEPTS_ARTIFACT_DIR` (si présent, prioritaire sur `artifactBaseDir`).

#### Anti-Patterns (à éviter)

- Recherche multi-emplacements implicite (fixtures → shared) dans le code des steps.
- Dénominations spéciales dans les noms de fichiers (la distinction se fait par le répertoire).
- Couplage fort aux chemins en dur dans les steps.

#### État d'implémentation

🚧 À implémenter dans les steps BDD et la configuration

### Format du template NuExtract généré par l'API

#### Description détaillée pour le format du template NuExtract

- [2025-10-21] L'API `/api/infer-template-async` transforme un schéma JSON Schema en template NuExtract avec des règles de conversion spécifiques
- [2025-10-21] **Fixture de référence** : Template valide obtenu de la plateforme SaaS disponible dans `__tests__/fixtures/nuextract-template-valid.json`
- [2025-10-21] **Types de base** : `"type": "string"` → `"string"`, `"type": "string", "pattern": "..."` → `"verbatim-string"`, `"type": "string", "format": "date"` → `"date-time"`
- [2025-10-21] **Énumérations** : Les `enum` JSON Schema sont converties en **tableaux** dans le template NuExtract (ex: `"enum": ["de", "fr", "it", "en"]` → `["de", "fr", "it", "en"]`)
- [2025-10-21] **Tableaux** : Les `"type": "array"` JSON Schema sont représentés avec **un seul élément type** dans le template
- [2025-10-21] **Références $ref** : Les références sont **résolues et intégrées** dans le template final (pas de `$ref` dans le template NuExtract)

#### Justification pour le format du template NuExtract

- **Interopérabilité** : Format standardisé NuExtract pour les extractions futures
- **Validation** : Template validé par la plateforme SaaS garantit la conformité
- **Tests** : Fixture de référence permet la validation automatisée des générations de template
- **Documentation par l'exemple** : Template valide sert de référence pour comprendre les transformations

#### Patterns validés pour le format du template NuExtract

- **Conversion énumérations** : `"language": {"type": "string", "enum": ["de", "fr", "it", "en"]}` → `"language": ["de", "fr", "it", "en"]`
- **Types NuExtract** : `"verbatim-string"` pour patterns, `"string"` pour texte libre, `"date-time"` pour dates
- **Résolution références** : `"concept-phases": {"$ref": "./hermes2022-phases.json"}` → structure complète intégrée
- **Arrays template** : `"phases": [{"id": "verbatim-string", "name": "string", ...}]` (un seul élément type)
- **Fixture de test** : `__tests__/fixtures/nuextract-template-valid.json` comme référence pour validation

#### Anti-Patterns pour le format du template NuExtract à éviter

- **$ref non résolues** : Éviter de laisser des `$ref` dans le template → **Solution** : Résoudre toutes les références avant envoi à l'API → **Règle à adopter** : Utiliser le schéma résolu complet
- **Énumérations non converties** : Éviter de tester avec `"enum": [...]` dans template → **Solution** : Valider que les énumérations sont des tableaux → **Règle à adopter** : Tests BDD vérifient la conversion
- **Structure array incorrecte** : Éviter plusieurs éléments types dans array → **Solution** : Un seul élément type → **Règle à adopter** : Format NuExtract standard

#### État d'implémentation pour le format du template NuExtract

✅ [Fixture validée et tests adaptés]

- ✅ **Fixture de référence** : `__tests__/fixtures/nuextract-template-valid.json` créée depuis plateforme SaaS
- ✅ **Tests BDD améliorés** : Validation de la structure, des énumérations converties, des types NuExtract, et de la résolution des `$ref`
- 🚧 **API retourne template vide** : Investigation en cours avec NuMind sur pourquoi l'API retourne `{}` alors que la plateforme SaaS fonctionne correctement
